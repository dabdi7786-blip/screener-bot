# Только для .github/workflows/radio_digest.yml — тяжёлые зависимости
# (ctranslate2/torch), намеренно не в основном requirements.txt, который
# ставится в bot_poll.yml каждые ~10 минут.
faster-whisper>=1.0
anthropic>=0.30
requests>=2.32
python-dotenv>=1.0
